from setuptools import setup, find_packages

setup(
    name='gravures_custom',
    version='0.0.1',
    description='Customizations for Gravures ERPNext',
    author='Mitesh',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
